accelerate==1.6.0
transformers==4.54.1
diffusers[torch]==0.32.1
ftfy==6.3.1
# albumentations==1.3.0
opencv-python==4.10.0.84
einops==0.7.0
pytorch-lightning==1.9.0
bitsandbytes==0.46.0
lion-pytorch==0.2.3
schedulefree==1.4
pytorch-optimizer==3.10.0
prodigy-plus-schedule-free==1.9.2
prodigyopt==1.1.2
# TensorBoard 2.10.x ships generated protobuf code that is incompatible with
# newer protobuf runtimes. TensorBoard 2.20.0 works with protobuf>=3.19.6,
# while open-clip-torch 2.20.0 requires protobuf<4 and wandb 0.16.2 requires
# protobuf>=3.19,<5. Pin 3.20.3 at the intersection of all three constraints.
protobuf==3.20.3
tensorboard==2.20.0
safetensors==0.4.5
# ONNX Runtime is intentionally managed by mikazuki.launch_utils.setup_onnxruntime
# so the selected CPU/GPU distribution matches the installed PyTorch CUDA stack.
# Do not add onnxruntime or onnxruntime-gpu here.
# gradio==3.16.2
altair==4.2.2
easygui==0.98.3
toml==0.10.2
voluptuous==0.15.2
huggingface-hub==0.34.3
# for Image utils
imagesize==1.4.1
# for T5/Qwen tokenizers (SD3/FLUX/Anima)
sentencepiece==0.2.1
# for ui
rich==14.1.0
pandas
scipy
requests
pillow
numpy==1.26.4
# <=2.0.0
gradio==3.44.2
fastapi==0.95.1
uvicorn==0.22.0
wandb==0.16.2
httpx==0.24.1
# extra
open-clip-torch==2.20.0
lycoris-lora==3.2.0.post2
dadaptation==3.1
